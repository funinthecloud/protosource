name: Release
on:
  push:
    tags: ['v*']

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: bufbuild/buf-action@v1
        with:
          token: ${{ secrets.BUF_TOKEN }}

      # Push proto module to BSR with version label
      - name: Push proto module
        env:
          BUF_TOKEN: ${{ secrets.BUF_TOKEN }}
        run: buf push --label ${GITHUB_REF_NAME}

      # Build and push plugin images
      - name: Login to BSR plugin registry
        run: echo "${{ secrets.BUF_TOKEN }}" | docker login plugins.buf.build -u _ --password-stdin

      - name: Build and push plugins
        run: |
          VERSION=${GITHUB_REF_NAME}
          docker build -f Dockerfile.protosource -t plugins.buf.build/funinthecloud/protosource:${VERSION} --platform linux/amd64 .
          docker build -f Dockerfile.protosource-ts -t plugins.buf.build/funinthecloud/protosource-ts:${VERSION} --platform linux/amd64 .
          docker push plugins.buf.build/funinthecloud/protosource:${VERSION}
          docker push plugins.buf.build/funinthecloud/protosource-ts:${VERSION}
